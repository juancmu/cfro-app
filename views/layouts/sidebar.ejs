  <!-- Sidebar Start -->

    <% areas.forEach(element => {  %>

   
  <%if( title == element.area){%>
    <% var activePs = "active" %>
    <%};  %>
      
      <%           });   %>
  <div class="sidebar pe-4 pb-3">
    <nav class="navbar bg-secondary navbar-dark nav-round">
      <a href="/" class="brand-link centrar">
        <img src="img/bg-red.png" alt="Logo" class="img-fluid nav-round centrar" style="opacity: .8" id="logo-sidebar-min">
        
      </a>
      
        <div class="navbar-nav w-100">
          <%if( title == "CFRO DASHBOARD"){%>
            <% var activeP = "active" %>
            <%};  %>
            <a href="/" class="nav-item nav-link <%= activeP %>"><i class="fa fa-tachometer-alt me-2"></i>Dashboard</a>
          
          <% areas.forEach(element => {  %>
                
                
                
              <li class="nav-item">
               
                <%if( title == element.area){%>
                  <% var activeP = "active" %>
                  <%};  %>

                <a href="/<%= element.area %>"  class="nav-item nav-link hover-<%= element.coloricon %> <%= activeP %>">

                  <i class="nav-icon text-<%= element.coloricon %> <%= element.icon %> "></i>
                                      <span class="text-light">
                                        <%= element.area %>

                                      </span>
                    <span class="right badge badge-danger"></span>
                    
                   
                </a>
              </li>
             
              <%           });   %>

          
        </div>
    </nav>
</div>

<!-- Sidebar End -->