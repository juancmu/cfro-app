  <!-- Sidebar Start -->

  <% var colors = []  %>
  <% var titles = []  %>
  <% var icons = []  %>
  <% var i = 0  %>

    <% areas.forEach(element => {  %>

        <% colors[element.area] = element.coloricon %>
        <% titles[element.area] = element.area %>
        <% icons[element.area] = element.icon %>
  
     
        <%if( title == element.area){%>
          <% var activePs = "active" %>
        <%};  %>
      
    <%           });   %>

  <div class="sidebar pe-4 pb-3">

    <%if( title == "CFRO DASHBOARD" || title == "GEOGRAPHIC INFORMATION SYSTEM"){%>

      <nav class="navbar bg-secondary bd-gray-dark navbar-dark nav-round">
      
        <%}else{;  %>

    <nav class="navbar bg-secondary bd-<%= colors[title]%> nav>navbar-dark nav-round">
      <%};  %>




      <a href="/" class="brand-link centrar">
        <img src="/../img/logo-red.png" alt="Logo" class="img-fluid nav-round centrar" style="opacity: .8" id="logo-sidebar-min">
      
      </a>
      
        <div class="navbar-nav w-100">
          <%if( title == "CFRO DASHBOARD"){%>
            <% var activeP = "active" %>
            <%};  %>
            <a href="/" class="nav-item nav-link hover-gray-dark <%= activeP %>"><i class="fa fa-tachometer-alt me-2"></i> 
              <span class="text-gray-pearl">
                Dashboard

              </span>  
            </a>
          
          <% areas.forEach(element => {  %>
                                
              <li class="nav-item">
               
                <%if( title == element.area){%>
                  <% var activeP = "active" %>
                <%};  %>

                <a href="/<%= element.area %>"  class="nav-item nav-link hover-<%= element.coloricon %> <%= activeP %>">

                  <i class="nav-icon text-<%= element.coloricon %> <%= element.icon %> fa-xl"></i>
                                      <span class="text-gray-pearl">
                                        <%= element.area %>

                                      </span>
                    <span class="right badge badge-danger"></span>
                    
                   
                </a>
              </li>
             
          <%  });   %>

          
        </div>
    </nav>
</div>

<!-- Sidebar End -->