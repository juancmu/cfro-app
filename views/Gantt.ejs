<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Getting Started with Chart JS with www.chartjs3.com</title>
   
  </head>
  <script>
    // ejs vars to js
  var schedule = <%- JSON.stringify(schedule); %>
  var sections = <%- JSON.stringify(sections); %>
  console.log(schedule);
</script>
  <script type="text/javascript" src="https://www.gstatic.com/charts/loader.js"></script>
  <script type="text/javascript">
    google.charts.load('current', {'packages':['gantt']});
    google.charts.setOnLoadCallback(drawChart);

    const dataChart = []
    const dataName = []
    var scheduleData = [] 
    const structures = []


    schedule.map(el => {
     
      
      scheduleData.push ([el['Activity Name'], el['Activity Name'], null, new Date(el['BL Project Finish']), new Date(el['BL Project Start']), null, el['ejecutado']*100, null])
      
      // ['2014Spring', 'Spring 2014', 'spring',
      //    new Date(2014, 2, 22), new Date(2014, 5, 20), null, 100, null],
      
    })  
    
    console.log(scheduleData);
    function drawChart() {
g
      var data = new google.visualization.DataTable();
      data.addColumn('string', 'Task ID');
      data.addColumn('string', 'Task Name');
      data.addColumn('string', 'Resource');
      data.addColumn('date', 'Start Date');
      data.addColumn('date', 'End Date');
      data.addColumn('number', 'Duration');
      data.addColumn('number', 'Percent Complete');
      data.addColumn('string', 'Dependencies');

      data.addRows(scheduleData);

      var options = {

        height: 900,
        gantt: {
          trackHeight: 30
        },
       
      };

      var chart = new google.visualization.Gantt(document.getElementById('chart_div'));

      chart.draw(data, options);
    }
  </script>
</head>
<body>
  <div id="chart_div" class="container-fluid"></div>
</body>
</html>